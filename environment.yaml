name: horses
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pandas
  - pip
  - pip:
    - playwright
    - pymongo[srv]
    - scrapy
    - scrapy-playwright
    - py3-validate-email
    - requests
